# Modesetting KMS: obligatorio para Wayland/Hyprland.
# fbdev=1: framebuffer de consola gestionado por el driver, necesario para que
#          los TTY y un kernel panic sean legibles con la 3080 en Wayland.
options nvidia_drm modeset=1 fbdev=1

# Preserva la VRAM al suspender/hibernar. En la rama 610 ya es el valor por
# defecto; se fija explicitamente para no depender de la version del driver.
#
# EnableGpuFirmware=0: desactiva el firmware GSP. Workaround para el crash
# recurrente Xid 120 (GSP task exception: load address misaligned) que tumbo
# Hyprland dos veces (2026-09-11 y 2026-09-16) mientras jugaba Marvel Rivals
# via Proton. Vuelve al modo de driver "legacy" sin GSP. Ver docs/omarchy.md.
options nvidia NVreg_PreserveVideoMemoryAllocations=1 NVreg_EnableGpuFirmware=0
